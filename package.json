{
  "name": "talks",
  "version": "1.0.0",
  "private": true,
  "description": "Presentation deck built on reveal.js",
  "license": "MIT",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "vendor": "rm -rf reveal && mkdir -p reveal && cp -R node_modules/reveal.js/dist reveal/dist && find reveal -name '*.d.ts' -delete && find reveal -type d -empty -delete"
  },
  "dependencies": {
    "reveal.js": "6.0.2"
  }
}
